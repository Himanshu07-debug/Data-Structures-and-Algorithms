#include<bits/stdc++.h>
using namespace std;

int maximumLength(vector<int>& arr) {

    int n = arr.size();

    unordered_map<int, int> m;

    for(int x : arr) m[x]++;

    int c = 0;

    int mx = 1;

    for(auto [a, b] : m){

        

    }

    return mx;
        
}